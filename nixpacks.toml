# ⚠️ DEPRECATED — no usar para nuevos deploys.
#
# Fuente de verdad de la landing: Vercel (pipelinex.app).
# Este archivo existe porque en algún momento se levantó un servicio Railway
# paralelo sirviendo dist/ con Caddy. Si aún está activo, apágalo: tener
# dos orígenes para la misma landing hace que las versiones diverjan.
#
# Si confirmas que ningún servicio Railway apunta a este repo, elimina este
# archivo junto con el servicio.

[phases.setup]
nixPkgs = ["caddy"]

[phases.install]
cmds = []

[phases.build]
cmds = []

[start]
cmd = "caddy file-server --root dist --listen :$PORT"
